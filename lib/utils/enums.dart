enum LoadingState {
  loading, paginating, done, empty, error, networkError
}
enum LocationStatus {
  enabled, serviceDisabled, noPermission
}
enum AlarmType {
  once, repeat, schedule
}

// enum NPPaymentStatus {
//   waiting,
//   confirming,
//   confirmed,
//   sending,
//   partially_paid,
//   finished,
//   failed,
//   refunded,
//   expired
// }

enum MD2IndicatorSize {
  tiny,
  normal,
  full,
}